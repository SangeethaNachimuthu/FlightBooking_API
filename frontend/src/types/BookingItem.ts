export type BookingItem = {
    id: number;
    passengerName: string;
    passengerEmail: string;
    flightNumber: string;
    destination: string;
    departureTime: string;
    arrivalTime: string;
    status: string;
}
